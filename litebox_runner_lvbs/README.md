# A LiteBox Runner for running LiteBox in Hyper-V VTL1 kernel space

> [!WARNING]
> This crate is work in progress. OP-TEE workloads cannot run on LVBS until a
> kernel broker platform is implemented to provide broker-backed services such
> as cryptographic randomness.
