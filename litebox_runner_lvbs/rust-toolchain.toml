[toolchain]
# Use a nightly toolchain that matches the workspace's stable version (1.94.x).
# Nightly is required for `-Z build-std` to compile core/alloc with SSE support.
# When updating stable, find a nightly date with the same version number.
channel = "nightly-2026-01-15"
